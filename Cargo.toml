[package]
name = "snapture"
version = "0.1.0"
edition = "2024"

[dependencies]
ashpd = { version = "0.13.5", features = ["screenshot"] }
tokio = { version = "1.50.0", features = ["macros", "rt-multi-thread"] }
